# CLAUDE.md — iNote.Money 프로젝트 지침

> 이 파일은 Claude Code가 자동으로 읽는 규칙 파일입니다.
> 목적: 토큰 효율 + 일관된 코드 품질 + 빠른 출시

---

## 📌 프로젝트 컨텍스트

- **프로젝트명**: iNote.Money
- **스택**: React / TypeScript / Next.js
- **개발자**: FE 4년차 솔로 개발
- **목표**: 완벽보다 빠른 출시 우선. 과설계 금지.
- **원칙**: 배운 걸 바로 적용. 결과 보고 개선.

---

## 🤖 응답 규칙 (토큰 절약)

- 인사말, 마무리 멘트 생략 ("물론이죠!", "도움이 되셨으면 좋겠습니다" 등 금지)
- 코드 수정 시 **변경된 부분만** 제시. 전체 파일 출력 금지.
- 설명은 **"왜"를 먼저**, 코드는 그 다음
- 모르거나 불확실하면 반드시 명시할 것
- 선택지가 필요할 땐 **2가지만** 제시 (3개 이상 금지)

---

## 🎯 작업별 접근 방식

### 정확성 우선 (Temperature 낮음)
> 계산 로직, 버그 수정, 데이터 처리, API 연동

- 한 가지 정답만 제시
- 엣지 케이스(빈값, 음수, 타입 오류 등) 반드시 언급
- 검증 방법도 함께 제시

### 균형 (Temperature 중간)
> 컴포넌트 설계, 아키텍처 결정, 코드 리뷰

- 2가지 안 비교 후 추천
- 추천 이유를 단계적으로 설명 (CoT 방식)
- 장단점 형식:
  ```
  안 A:
  - 장점: ...
  - 단점: ...
  
  안 B:
  - 장점: ...
  - 단점: ...
  
  추천: ...
  ```

### 창의성 우선 (Temperature 높음)
> 네이밍, 카피라이팅, 아이디어 브레인스토밍

- 3가지 이상 옵션 제시
- 형식 통일 없이 자유롭게

---

## 🔧 복잡한 작업 처리 방식 (CoT 자동 적용)

아키텍처 결정, 버그 분석, 기술 선택 시 반드시 아래 순서로:

```
1. 문제/상황 파악
2. 선택지 또는 원인 나열
3. 각각 장단점 분석
4. 최종 추천 + 이유
```

---

## 📐 코드 컨벤션

### 네이밍
```
컴포넌트:  PascalCase           예) UserProfileCard
훅:        use + 동사            예) useTransactionList
유틸함수:  camelCase             예) formatCurrency
상수:      UPPER_SNAKE_CASE      예) MAX_TRANSACTION_AMOUNT
타입/인터페이스: PascalCase + 접미사  예) TransactionType, UserProps
```

### 커밋 메시지
```
형식: type: 한국어 설명

feat: 지출 입력 기능 추가
fix: 합계 계산 오류 수정
refactor: 트랜잭션 훅 분리
chore: 패키지 버전 업데이트
```

### 컴포넌트 구조
```typescript
// 순서 고정
1. import
2. 타입 정의
3. 컴포넌트 함수
4. export
```

---

## 💰 토큰 효율 규칙

- 파일 전체 읽기 전에 **필요한 함수/컴포넌트만** 요청
- 코드 리뷰는 **함수 단위**로 (파일 전체 금지)
- 긴 대화 후 새 주제는 **새 채팅**으로 분리
- 반복되는 맥락은 이 파일에 추가해서 매번 설명 생략

---

## 🚫 금지 사항

- 과설계 금지 — MVP 먼저, 확장은 나중에
- 완벽한 코드 추구 금지 — 동작하는 코드 우선
- 라이브러리 남용 금지 — 네이티브로 가능하면 네이티브로
- Git push 자동 실행 금지 — 반드시 확인 후 수동 push

---

## 📝 작업 요청 템플릿

### 버그 수정 요청 시
```
환경: [스택/버전]
증상: [어떤 상황에서 어떻게 발생]
예상 원인: [있으면 작성]
관련 코드: [해당 함수/컴포넌트만]
```

### 기능 구현 요청 시
```
목적: [왜 필요한지]
입력: [받는 데이터]
출력: [결과물]
제약: [있으면 작성]
```

### 코드 리뷰 요청 시
```
리뷰 포인트: [성능 / 가독성 / 버그 / 전체]
관련 코드: [함수 단위로]
```
